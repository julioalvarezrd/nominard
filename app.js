const RULES={afpRate:.0287,sfsRate:.0304,afpCap:464460,sfsCap:232230,isr:[416220.01,624329.01,867123.01]};const $=id=>document.getElementById(id);const money=n=>n.toLocaleString('es-DO',{style:'currency',currency:'DOP'});const num=v=>Number(String(v||0).replace(/,/g,''))||0;function calcISR(monthly){const annual=Math.max(0,monthly)*12,[a,b,c]=RULES.isr;if(annual>c)return((annual-c)*.25+79776)/12;if(annual>b)return((annual-b)*.20+31216)/12;if(annual>a)return((annual-a)*.15)/12;return 0}function calculate(){const salary=num($('salary').value),tips=num($('tips').value),commissions=num($('commissions').value),other=num($('otherIncome').value);if(salary<=0){$('salary').focus();return}const tssSalary=salary+commissions;const afp=Math.min(tssSalary,RULES.afpCap)*RULES.afpRate;const sfs=Math.min(tssSalary,RULES.sfsCap)*RULES.sfsRate;const gross=salary+tips+commissions+other;const taxable=Math.max(0,gross-afp-sfs);const isr=calcISR(taxable);const deductions=afp+sfs+isr;const net=gross-deductions;const values={net,gross,rSalary:salary,rTips:tips,rCommissions:commissions,rOther:other,afp,sfs,isr,deductions,tssBase:tssSalary,isrBase:taxable,fortnight:net/2};Object.entries(values).forEach(([id,v])=>$(id).textContent=money(v));if($('netStrip'))$('netStrip').textContent=money(net);if($('grossDonut'))$('grossDonut').textContent=money(gross)}document.addEventListener('DOMContentLoaded',()=>{$('payrollForm').addEventListener('submit',e=>{e.preventDefault();calculate()});$('payrollForm').addEventListener('reset',()=>setTimeout(()=>['net','gross','rSalary','rTips','rCommissions','rOther','afp','sfs','isr','deductions','tssBase','isrBase','fortnight','netStrip','grossDonut'].forEach(id=>{if($(id))$(id).textContent=money(0)}),0));if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js').catch(()=>{})});